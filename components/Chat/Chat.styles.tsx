import styled from "styled-components";
import styledMap from "styled-map";

type DataProps = {
}

export const ChatStyle = styled.div<DataProps>`

`;

export const ChatMain = styled.div<DataProps>`

`